{
  "private": true,
  "engines": {
    "node": ">= 6"
  },
  "options": {
    "storage": "$HOME/Google Drive/.dotfiles/*"
  },
  "scripts": {
    "sn": "sn \"$npm_package_options_storage\" mackup* $HOME --hidden",
    "start": "sh ./bootstrap.sh && npm run sn $& sh macos.sh"
  }
}
